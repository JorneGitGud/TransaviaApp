const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>My Travel Planner</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/">Tickets</a>
                <a href="/">About</a>
            </div>
        </nav>

    );
}

export default Navbar;